・ルートアクセス時の挙動
ログインしていなければ、/loginへ飛ばす
ログインしていれば、ダッシュボードに飛ばす。

・セッションの管理
ログイン時にトークンを発行し、CookieとDBに保存
トークンには有効期限を設定し、DB側で時間を管理
DB側で「トークンは有効と判断」した場合は、各操作を実行可能
※引数には、トークンを記述
トークンが無効な条件は、
「トークンのflagがFalseの時」
「トークンがそもそもない時」
のいずれか

サーバ起動時は、初期処理として「全トークンの削除」を行う。
→専用テーブルの再生成でいい？

pcc_userのDB書式
display,name,email,isAdmin,solt,passwd,activate_flag,uuid,accessToken,grade,class,discord

アクセストークンの形式
ランダムに生成した文字列

アクセストークンは、各セッションにて一意である必要がある。
トークンの生成はサーバ側で行う。
→ログイン成功時にクライアントへクッキーとして保存
一定時間経過時にDBからトークン情報を削除

パスワードを忘れた場合の処理
何かフォームが欲しい。
もしくは、自分でパスワードリセットができるとか
→学校GmailもしくはDiscordDMに対してワンタイムパスワードを送信とか

備品リストのレコード
number,item_name,desc,resource,rental,picture
借りている備品リストのレコード
number,item_name,use,rentby,rent,deadline,returned

貸し出し、返却ボタンについて
・全てのテーブルを対象に一度追加する
・その後にgetElementbyId()をして、全ボタンを取得
・状況が想定する状態の場合にvisiblityをvisibleに変更
動作について(返却)
・クリックした瞬間、DBからrental_idから情報を引っ張ってきて
　モーダルを用いて画面に返却備品の情報を表示
・モーダルの中の「返却する」を押すと、返却処理が行われる
動作について(貸し出し)
・クリックした瞬間、対象の備品情報を取得してモーダルに表示
・「借りる」ボタンを押すとDBの書き換え処理が行われる

備品登録の作業
・管理者アカウントのみが備品の登録できる
・備品情報の編集機能も欲しいね
・写真を追加できる機能も欲しいかも？

「「「「「完成！！！！！！！！！！！！！！！！！！！！」」」」」